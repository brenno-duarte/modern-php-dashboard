<div class="row mb-3">
    <div class="form-check">
        <input class="form-check-input" type="{{type}}" {{name}} {{value}} {{id}} {{options}}>
        <label class="form-check-label" for="{{for}}">{{label}}</label>
    </div>
</div>
