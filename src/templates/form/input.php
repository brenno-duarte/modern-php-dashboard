<div class="row mb-3">
    <label for="{{for}}" class="col-sm-2 col-form-label">{{label}}</label>
    <div class="col-sm-10">
        <input type="{{type}}" class="form-control" {{name}} {{id}} {{value}} {{options}}>
    </div>
</div>
